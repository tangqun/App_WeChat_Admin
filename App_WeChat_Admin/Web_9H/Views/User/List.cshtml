@model PagedList<Model_9H.UserInfoModel>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="main-hd">
    <h2>用户管理</h2>
    @*<div class="tab-box">
            <ul class="tab-inner">
                <li class="tab-item active">
                    <a href="/app/material/newslist">图文消息</a>
                </li>
                <li class="tab-item">
                    <a href="/app/material/imagelist">图片</a>
                </li>
            </ul>
        </div>*@
</div>
<div class="main-bd">
    <div class="search-box">
        @*<form class="form-inline">
                <div class="form-group">
                    <label class="sr-only" for="exampleInputEmail3">Email address</label>
                    <input type="text" name="key" id="key" class="form-control" placeholder="标题/作者/摘要">
                </div>
                <button type="submit" class="btn btn-default">查询</button>
            </form>*@
        @using (Ajax.BeginForm("list", null, new AjaxOptions { UpdateTargetId = "js_view", InsertionMode = InsertionMode.Replace, HttpMethod = "POST" }, new { id = "searchForm", @class = "form-inline" }))
        {

        }
    </div>

    <div id="js_view">
        @Html.Partial("_AjaxSearchPost", Model)
    </div>
</div>

@section js {
    @{Html.RegisterMvcPagerScriptResource();}
    <script src="../../plugins/jquery-ajax-unobtrusive/jquery.unobtrusive-ajax.js"></script>
}
