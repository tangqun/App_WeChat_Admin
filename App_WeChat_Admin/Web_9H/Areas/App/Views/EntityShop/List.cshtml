@model PagedList<Model_9H.EntityShopInfoModel>
@{
    Layout = "~/Areas/App/Views/Shared/_Layout.cshtml";
}

<div class="height-50 line-height-50 b margin-bottom-15">
    门店管理
</div>

@using (Ajax.BeginForm("list", null, new AjaxOptions { UpdateTargetId = "js_view", InsertionMode = InsertionMode.Replace, HttpMethod = "POST" }, new { id = "searchForm", @class = "form-inline margin-bottom-15" }))
{
    @*<div class="form-group">
        <label for="key" class="sr-only">手机号/真是姓名</label>
        <input type="text" name="key" class="form-control" id="key" placeholder="手机号/真是姓名">
    </div>
    <button type="submit" class="btn btn-default">查询</button>*@
}
<div id="js_view">
    @Html.Partial("_AjaxSearchPost", Model)
</div>

@section js {
    @{Html.RegisterMvcPagerScriptResource();}
    <script src="../../plugins/jquery-ajax-unobtrusive/jquery.unobtrusive-ajax.js"></script>
}